<manifest xmlns:android="http://schemas.android.com/apk/res/android"

          package="net.droidman.librate"
    >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        >
        <activity android:name=".rate.RateImeFirstActivity"/>
        <activity android:name=".rate.RateImeRateActivity"/>
        <activity android:name=".rate.RateImeSecondActivity"/>
        <activity android:name=".rate.RateImeFeedbackActivity"/>
    </application>

</manifest>
